# Project Governance

This repository is the long-term source of truth for design, implementation, test evidence, operations, and future iterations of Baby Monitor Local.

Stable documentation and released code live on `main`. Development uses focused `codex/...` branches and pull requests. Public artifacts must never contain household media or secrets.
